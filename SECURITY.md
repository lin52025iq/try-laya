# Security boundaries and limitations

这是本机实验 Agent，不是经过审计的浏览器沙箱或实时安全控制器。只在自己授权的设备、测试页面和应用中运行；勿连接有不可恢复副作用的账号来做无监督测试。

## 默认限制

- CLI 仅绑定 127.0.0.1；API 使用启动令牌，WebSocket 在首条消息认证；拒绝外源请求和不允许的 Host。
- Session 创建不会自动运行；默认 Assist。自动点击/提交和短时控制需用户额外授权；Profile/LLM 无权改变这些权限。
- 没有任意 shell、文件读写、MCP 或代码执行工具接口。外部策略是严格 schema 的数据，不是 Python/JS 插件。
- 事务型动作检查目标身份和语义指纹；目标替换、填充值变化、版本改变都会使旧动作失效。
- 推理不持有执行锁，超时结果不能派发。执行结果不明确时停止，不盲目重试。重启不会恢复旧 Session 或重放日志。
- basic ADB 文本限制 ASCII 字符，使用 argv 启动且再限制设备 shell 参数；拒绝 shell 元字符。没有 FPS 多点控制伪实现。

## 不应误解为已经解决的事情

`allow_auto_click=true` 是较宽的会话授权，并不是付款/删除/发信等语义风险都已识别。Laya 的概率、页面文字和 LLM 判断不能作为最终安全边界。敏感或真实账号流程应保持 Assist 并逐步确认。

页面 DOM、caption、攻略和模型输出都不可信。上下文标注不等于消除了 prompt injection。截图和网页文本可能含个人数据。表单 facts 不发给策略模型，但用户主动写进目标、攻略或网页展示内容中的秘密无法被本版通用识别。

Browser 的 origin 路由限制不是操作系统网络沙箱；不支持的弹窗、已有 Service Worker、WebRTC、扩展等需要进一步隔离。Attached Chrome 使用已有 context，隔离更弱，所以只允许 Assist/Manual，只拥有新建标签，不拦截用户其他标签的行为。

Human takeover 通过本服务 Console/API 生效；目前没有全局 OS 键鼠钩子。外部直接点击窗口前先接管。已发送到底层的动作无法回滚。控制 TTL 依赖进程/事件循环/驱动正常调度，不保证系统崩溃时按键一定释放；真实设备高频控制必须配独立设备端 watchdog。

## 本地数据

访问令牌仅在终端显示和浏览器 sessionStorage（可用时）保存；不要共享它。默认随机生成，可用 `AGENT_TOKEN` 配置；不要提交到 Git。所有API响应 no-store，Console禁止被iframe嵌入。

默认事件记录 metadata，不记录 facts 值。开启 `AGENT_RECORD_TRAINING=1` 后会记录状态/候选文本，请按敏感数据处理。手动截图可能包含密码后的页面、订单或个人信息；仅本地保存，不要推送。数据库当前不加密，也没有自动清理/删除策略。

## 发生异常

控制台接管或停止；必要时关闭服务拥有的浏览器实例。不要在结果 unknown 时自动重试提交。保存不含敏感信息的事件 ID、错误码、软件版本和最小复现；检查端到端延迟，而非仅观察模型耗时。
