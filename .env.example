# Examples only: this file is NOT loaded automatically.
# Set environment variables in your shell or process supervisor; never commit real keys.
AGENT_POLICY=local
LAYA_MODEL=multilingual
LAYA_DEVICE=cpu
AGENT_DATA_DIR=.data
AGENT_RECORD_TRAINING=0
# AGENT_TOKEN=<optional fixed local access token; otherwise generated on startup>
# BROWSER_EXECUTABLE=/path/to/chromium
# LAYA_BASE_URL=http://127.0.0.1:8000
# LAYA_API_KEY=<only when using authenticated upstream laya-serve>
# LLM_BASE_URL=http://127.0.0.1:1234/v1
# LLM_MODEL=<your installed model>
# LLM_API_KEY=<optional>
